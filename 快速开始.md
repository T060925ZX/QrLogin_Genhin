# 🚀 快速开始指南

## 第一次使用? 只需3步!

### 步骤 1️⃣: 安装依赖

```bash
pip install opencv-python pyzbar Pillow numpy
```

**注意**: pyzbar 需要系统安装 zbar 库
- Windows: 下载 https://github.com/npinchot/zbar/releases

---

### 步骤 2️⃣: 配置 stoken.txt

1. 复制 `stoken.txt.example` 为 `stoken.txt`
2. 编辑 `stoken.txt`，填入你的cookie

**如何获取Cookie:**
1. 浏览器打开 https://account.mihoyo.com/
2. 登录账号
3. 按 F12 → Network标签 → 刷新页面
4. 点击任意请求 → 复制 Request Headers 中的 cookie
5. 粘贴到 `stoken.txt` 中

**格式示例:**
```
stuid=260995738;stoken=v2_...;ltoken=...;mid=...;
```

**注意:** 
- uid会自动从cookie中提取（stuid）
- 其他配置已写死在代码中，无需修改

---

### 步骤 3️⃣: 启动程序

```bash
python main.py
```

---

## 📱 使用流程

1. **运行程序后** → 屏幕中央会出现红色方框，预览窗口在右上角
2. **打开原神PC客户端** → 选择扫码登录
3. **移动游戏窗口** → 让二维码对准红色方框
4. **自动识别** → 程序会自动抢码并登录
5. **完成!** → 看到成功提示，游戏已自动登录

---

## ✅ 检查清单

使用前确认:
- [ ] Python 3.7+ 已安装
- [ ] 依赖包已安装
- [ ] stoken.txt 已创建并填写cookie
- [ ] kuang.py 文件存在
- [ ] 网络连接正常

---

## 🔍 问题排查

### 提示 "未找到stoken.txt文件"?
**A**: 创建 `stoken.txt` 文件并填入cookie

### 提示 "stoken.txt格式错误"?
**A**: 确保cookie包含 `stuid=xxx` 字段

### 二维码识别不到?
**A**: 
- 调整游戏窗口位置
- 确保二维码完全在红框内
- 增加屏幕亮度

### 抢码失败?
**A**: 
- 检查cookie是否过期
- 验证stuid是否正确
- 查看控制台错误信息

---

## 💡 小贴士

1. **Cookie有效期**: cookie会过期，如果登录失败可能需要重新获取
2. **扫描框位置**: 红框始终在屏幕中央，预览窗口在右上角
3. **退出程序**: 在扫描窗口按任意键即可退出
4. **配置文件**: 只需修改 `stoken.txt`，其他配置已写死

---

**准备好了吗? 开始吧! 🎮**
